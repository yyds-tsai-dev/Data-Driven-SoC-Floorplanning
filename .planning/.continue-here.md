---
context: default
phase: server-migration
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-08-22T01:11:19+08:00
---

<current_state>

No active goal exists in the goal service. Repository consolidation and branch reachability are complete on local `main`; final verification is recorded and GitHub push is blocked only by missing credentials.

</current_state>

<completed_work>

- Audited every registered worktree and compared all named branches against `main` and `5.6-sol-reduce-time`.
- Confirmed `5.6-sol-flow-matching` is already an ancestor and five agent feature commits are patch-equivalent to integrated commits.
- Preserved official beta materials, frontend/backend reports, topology probes/evidence, runtime harnesses, and small dirty-worktree evidence in focused commits.
- Ran the full pre-merge suite: 2,232 passed, 5 skipped, 17 pre-existing scorer-contract failures.
- Ran syntax/container checks and the focused backend suite: 63 passed.
- Merged all requested worktree/feature branches and proved all 11 heads reachable from `main`.
- Preserved the full flow-v1 continuation checkpoint and newest groupbridge submission package through exact Git LFS rules.
- Passed 289 merged-feature tests, 86 tests in the anytime pollution prefix, 30 anytime tests, submission validation, LFS fsck, diff checks, and Graphify update.
- Final merged-main full suite: 2,260 passed, 5 skipped, and only the 17 documented pre-existing topology scorer-import failures.

</completed_work>

<remaining_work>

- Authenticate GitHub, fetch/review any remote movement, and push local `main` without force, including LFS objects.
- Resolve or explicitly re-baseline the 17 topology scorer-import failures before claiming a green full suite.
- Separately transfer local-only generated data if required on the new server.
- Resume research only at the documented topology-prior G1 sequence.

</remaining_work>

<decisions_made>

- Use `5.6-sol-reduce-time` as the integration trunk because it already contains most branch content.
- Preserve no-go experiment code/history but do not enable it as production policy.
- Track only the requested full flow-v1 final checkpoint through Git LFS; keep periodic snapshots and FloorSet data outside ordinary Git.
- Do not repeat formal topology G0-v2; the next research gate is the not-yet-executed G1 sequence.

</decisions_made>

<blockers>

- GitHub credentials: HTTPS cannot prompt, `gh` is absent, and the available SSH key is not authorized.
- The verified non-force push attempt failed before transfer with `could not read Username`; no remote state changed.
- Full-suite baseline: 17 scorer-import/frozen-contract failures in `tests/test_icdc_topology_prior.py`.

</blockers>

## Required Reading (in order)

1. `docs/project-status/2026-08-22-server-migration-handoff.md` — detailed completed/remaining state, branch audit, test evidence, and generated-data transfer paths.
2. `.superpowers/sdd/progress.md` — historical implementation and gate decisions.
3. `docs/experiments/2026-08-14-g0-v2-transient-fp.md` — sealed G0 result and authorized next action.
4. `docs/superpowers/specs/2026-08-13-tfdl-data-free-topology-prior-design.md` — frozen topology-prior authority and safety contract.
5. `docs/superpowers/plans/2026-08-13-tfdl-data-free-topology-prior.md` — remaining G1 execution sequence.

## Critical Anti-Patterns (do NOT repeat these)

- Do not rerun G0-v2 or killed/no-go experiments as if they were incomplete; resume only the explicitly authorized G1 steps.
- Do not claim the full suite is green while the evaluator module resolves from the wrong path.
- Do not add generated nested checkpoints or `FloorSet/floorset_lite` to ordinary Git.
- Do not force-push or delete harness-owned worktrees during migration.

## Infrastructure State

- No background process from this workflow is running.
- Origin uses GitHub HTTPS but has no usable credential in this environment.
- Local-only data includes roughly 57 GB artifacts, 24 GB FloorSet lite data, and the remaining periodic flow-worktree snapshots. The full v1 final checkpoint and newest submission package are LFS-tracked but not remotely uploaded yet.

<context>

The integration used content-aware conflict resolution: current production behavior won over stale patch-equivalent worktree copies, while explicit branch ancestry, no-go experiment source, reports, tests, and small evidence were preserved. Local `main` is the handoff point.

</context>

<next_action>

Start with: configure GitHub authentication, run `git fetch origin`, inspect `main...origin/main`, and push local `main` without force. Then restore any required local-only artifacts and resume topology-prior G1 from the required-reading documents.

</next_action>
