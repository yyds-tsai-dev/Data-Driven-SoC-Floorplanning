---
context: default
phase: server-migration
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-08-22T00:30:47+08:00
---

<current_state>

No active goal exists in the goal service. Repository consolidation is in progress on `5.6-sol-reduce-time`; inventory, baseline verification, and preservation commits are complete, while branch merges, final verification, and push remain.

</current_state>

<completed_work>

- Audited every registered worktree and compared all named branches against `main` and `5.6-sol-reduce-time`.
- Confirmed `5.6-sol-flow-matching` is already an ancestor and five agent feature commits are patch-equivalent to integrated commits.
- Preserved official beta materials, frontend/backend reports, topology probes/evidence, runtime harnesses, and small dirty-worktree evidence in focused commits.
- Ran the full pre-merge suite: 2,232 passed, 5 skipped, 17 pre-existing scorer-contract failures.
- Ran syntax/container checks and the focused backend suite: 63 passed.

</completed_work>

<remaining_work>

- Merge the unique refine-kernel branch, the five ancestry-only agent branches, and all three `feat/*` no-go experiment branches.
- Fast-forward `main`, prove all worktree heads are reachable, update Graphify, and run final verification.
- Update handoff hashes and push `main`; GitHub authentication is currently unavailable.
- Separately transfer local-only generated data if required on the new server.

</remaining_work>

<decisions_made>

- Use `5.6-sol-reduce-time` as the integration trunk because it already contains most branch content.
- Preserve no-go experiment code/history but do not enable it as production policy.
- Do not commit approximately 270 GB of generated checkpoints/FloorSet data to ordinary Git.
- Do not repeat formal topology G0-v2; the next research gate is the not-yet-executed G1 sequence.

</decisions_made>

<blockers>

- GitHub credentials: HTTPS cannot prompt, `gh` is absent, and the available SSH key is not authorized.
- Full-suite baseline: 17 scorer-import/frozen-contract failures in `tests/test_icdc_topology_prior.py`.

</blockers>

## Required Reading (in order)

1. `docs/project-status/2026-08-22-server-migration-handoff.md` — detailed completed/remaining state, branch audit, test evidence, and generated-data transfer paths.
2. `.superpowers/sdd/progress.md` — historical implementation and gate decisions.
3. `docs/experiments/2026-08-14-g0-v2-transient-fp.md` — sealed G0 result and authorized next action.
4. `docs/superpowers/specs/2026-08-13-tfdl-data-free-topology-prior-design.md` — frozen topology-prior authority and safety contract.
5. `docs/superpowers/plans/2026-08-13-tfdl-data-free-topology-prior.md` — remaining G1 execution sequence.

## Critical Anti-Patterns (do NOT repeat these)

- Do not rerun G0-v2 or killed/no-go experiments as if they were incomplete; resume only the explicitly authorized G1 steps.
- Do not claim the full suite is green while the evaluator module resolves from the wrong path.
- Do not add generated nested checkpoints or `FloorSet/floorset_lite` to ordinary Git.
- Do not force-push or delete harness-owned worktrees during migration.

## Infrastructure State

- No background process from this workflow is running.
- Origin uses GitHub HTTPS but has no usable credential in this environment.
- Local-only data includes roughly 57 GB artifacts, 24 GB FloorSet lite data, and 246 GB flow-worktree checkpoints.

<context>

The integration strategy is content-aware: merge the one genuinely missing agent feature, preserve explicit branch ancestry for already patch-equivalent work, resolve stale feature-branch conflicts toward the newest integration behavior, and retain experiment reports as historical evidence.

</context>

<next_action>

Start with: verify `git status --short --branch`, merge `worktree-agent-a4125c5b5eebac69a` into `5.6-sol-reduce-time`, then merge the remaining audited branches in the order recorded in the migration plan.

</next_action>
