# Complete environment for the cadc1013 package (Case B: the evaluation venv is
# built from THIS file only, and it must also run the contest evaluator itself).
# torch is pinned to a CUDA-12 default wheel so the GPU works on any driver
# >= R525; the unpinned PyPI wheel (2.13+cu130) needs driver >= 580.
torch==2.6.0
numpy>=1.26.4,<2.3
scipy>=1.13.0
numba>=0.61.0
shapely>=2.0.0
matplotlib>=3.7.0
tqdm>=4.60.0
requests>=2.28.0
threadpoolctl>=3.1.0
